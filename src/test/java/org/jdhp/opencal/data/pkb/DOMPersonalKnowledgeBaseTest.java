package org.jdhp.opencal.data.pkb;

import org.jdhp.opencal.data.pkb.DOMPersonalKnowledgeBase;

import org.jdhp.opencal.model.card.Card;
import org.jdhp.opencal.model.cardcollection.CardCollection;

import static org.junit.jupiter.api.Assertions.assertEquals;

import org.junit.jupiter.api.Test;

class DOMPersonalKnowledgeBaseTest {

    @Test
    void myFirstTest() {
        //String path = DOMPersonalKnowledgeBaseTest.class.getResourceAsStream(name);

        //CardCollection cardCollection = load(URI uri) throws PersonalKnowledgeBaseException
        //for(Card card : cardCollection) {
        //    assertEquals(card.getQusetion(), card.getAnswer());
        //}

        assertEquals(2, 1 + 1, "1 + 1 should equal 2");
    }

}
